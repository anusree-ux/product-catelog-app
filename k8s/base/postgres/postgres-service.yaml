apiVersion: v1
kind: Service

metadata:
  name: postgres
  namespace: product-catalog
  labels:
    app: postgres
    component: database
    project: product-catalog

spec:
  selector:
    app: postgres

  ports:
    - port: 5432
      targetPort: 5432

  type: ClusterIP
